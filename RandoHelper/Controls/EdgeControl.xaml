<UserControl x:Class="RandoHelper.Controls.EdgeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RandoHelper.Controls"
             xmlns:model="clr-namespace:RandoHelper.Models"
             mc:Ignorable="d" 
             Background="LightGray"
             d:DesignHeight="250"
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance model:ModuleEdge, IsDesignTimeCreatable=True}"
             >
    <Border Margin="5"
            BorderBrush="Black"
            BorderThickness="1">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Content="{Binding CommonName}" Grid.Column="0" Grid.Row="0" />
        </Grid>
    </Border>
</UserControl>
